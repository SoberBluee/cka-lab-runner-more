apiVersion: v1
kind: Service
metadata:
  name: {{ include "cka-webapp.fullname" . }}
  labels:
    {{- include "cka-webapp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
  - port: {{ .Values.service.port }}
    targetPort: http
    protocol: TCP
    name: http
  selector:
    {{- include "cka-webapp.selectorLabels" . | nindent 4 }}
